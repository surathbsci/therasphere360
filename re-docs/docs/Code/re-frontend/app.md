---
sidebar_position: 1
---

# index

Index is the main entrypoint into running the React app

![Main Dependency Graph](/docs/entire-app.svg "Index")

The OIDC authentication configuration is injected into the application

React Router Dom is also setup here with links to the [`ActivityCalculatorView`](app/views/ActivityCalculatorView), `RedirectView` , `Logout`, `VialSelectorView` and `UnauthorizedView`

// TODO: Logout View needs to be renamed
